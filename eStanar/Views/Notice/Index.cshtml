@using eStanar.ViewModel
@model NoticeViewModel

@{
    ViewBag.Title = "Obavijesti";
}

<ul class="list-group">
    @foreach (var n in Model.NoticeDetailsList)
    {
        @Html.Partial("NoticeItem", new NoticeDetailsItemViewModel() { NoticeDetails = n, HideMoreDetailsLink = false })
    }
</ul>